# 用户系统与金币计费 — 详细开发任务清单

> 总计 5 个 Phase，预估总工时 2-3 周。每个 Phase 内部按顺序执行。

---

## Phase 1 — 地基层：数据库 + 认证系统

### 1.1 数据库搭建
- [ ] 在服务器上安装配置 PostgreSQL（或通过宝塔面板安装）
- [ ] 在 `.env` 中配置 `POSTGRES_PRISMA_URL` 和 `POSTGRES_URL_NON_POOLING`
- [ ] 扩展 `src/server/prisma/schema.prisma`：
  - [ ] 新增 `User` 模型（含 coinBalance 默认值 1）
  - [ ] 新增 `CoinTransaction` 模型
  - [ ] 新增 `ModelPricing` 模型
  - [ ] 新增 `RefreshToken` 模型
  - [ ] 新增 `PaymentOrder` 模型
  - [ ] 新增所有 Enum 类型（UserRole, TransactionType, ModelCategory, PayChannel, OrderStatus）
- [ ] 执行 `npx prisma generate` + `npx prisma db push` 同步至数据库
- [ ] 编写种子脚本 `prisma/seed.ts` 插入初始模型定价数据（5个模型）和默认管理员账号

### 1.2 JWT 认证基础设施
- [ ] 安装依赖：`jsonwebtoken`, `bcryptjs`, `@types/jsonwebtoken`, `@types/bcryptjs`
- [ ] 新建 `src/server/auth/jwt.ts`：JWT 签发/验证工具函数
  - `signAccessToken(userId, role)` → 15分钟有效
  - `signRefreshToken(userId)` → 7天有效
  - `verifyAccessToken(token)` → 返回 { userId, role }
- [ ] 新建 `src/server/auth/password.ts`：密码哈希/验证工具
  - `hashPassword(plain)` → bcrypt hash
  - `verifyPassword(plain, hash)` → boolean
- [ ] 在 `.env` 中新增 `JWT_SECRET` 和 `JWT_REFRESH_SECRET` 环境变量
- [ ] 在 `env.server.ts` 中注册这两个新环境变量

### 1.3 认证 tRPC Router
- [ ] 新建 `src/server/trpc/routers/auth.router.ts`：
  - [ ] `register` 过程：校验邮箱唯一 → 哈希密码 → 创建User(coinBalance=1) → 记录赠送流水 → 签发双Token
  - [ ] `login` 过程：校验邮箱密码 → 签发双Token → 存RefreshToken
  - [ ] `refresh` 过程：验证refreshToken有效 → 签发新accessToken
  - [ ] `logout` 过程：删除RefreshToken记录
  - [ ] `me` 过程：从JWT读userId → 返回用户信息+余额
- [ ] 在 `trpc.router-edge.ts` 中注册 `auth` 路由

### 1.4 tRPC Auth 中间件
- [ ] 新建 `src/server/trpc/middleware/authMiddleware.ts`：
  - 从请求 Header 提取 Bearer Token
  - 验证 JWT → 注入 `ctx.userId` 和 `ctx.userRole`
  - 无效/过期 Token → 返回 401
- [ ] 创建 `protectedProcedure`（需登录）和 `adminProcedure`（需Admin角色）
- [ ] **暂不**对现有路由强制应用（避免破坏开发中的功能）

---

## Phase 2 — 计费层：金币扣费 + 模型定价

### 2.1 金币服务层
- [ ] 新建 `src/server/services/coin.service.ts`：
  - [ ] `getBalance(userId)` — 查询用户余额
  - [ ] `checkBalance(userId, modelId)` — 预检查余额是否够用
  - [ ] `deductCoins(userId, modelId, description)` — 原子性扣币+记流水
    - 使用 Prisma `$transaction` 保证原子操作
    - 更新 User.coinBalance，插入 CoinTransaction
  - [ ] `addCoins(userId, amount, type, description)` — 充值/赠送加币

### 2.2 模型定价服务
- [ ] 新建 `src/server/services/pricing.service.ts`：
  - [ ] `getModelPrice(modelId)` — 查询模型单价
  - [ ] `getAllPricing()` — 查询所有定价
  - [ ] `upsertPricing(modelId, data)` — 新增/修改定价

### 2.3 CoinGuard 中间件
- [ ] 新建 `src/server/trpc/middleware/coinGuardMiddleware.ts`：
  - 从请求中提取 modelId
  - 调用 `checkBalance` 预检查
  - 不足 → 返回 `{ code: 'INSUFFICIENT_BALANCE', required, current }`
  - 足够 → 放行，调用后执行 `deductCoins`

### 2.4 AIX 路由注入扣费
- [ ] 修改 `src/modules/aix/server/api/aix.router.ts`：
  - 在聊天生成流程中注入 `authMiddleware` + `coinGuardMiddleware`
  - 成功完成后调用 `deductCoins`
- [ ] 修改 `backend/main.py` 生图接口：
  - 接收前端传来的 `userId` + 内部 token 验证
  - 成功生图后调用 Next.js 端的扣费 API

### 2.5 金币查询 tRPC Router
- [ ] 新建 `src/server/trpc/routers/coin.router.ts`：
  - [ ] `getBalance` — 查余额
  - [ ] `getTransactions` — 分页查流水（支持按类型筛选）
  - [ ] `checkBalance` — 前端预检查某模型余额
- [ ] 在 `trpc.router-edge.ts` 中注册 `coin` 路由

---

## Phase 3 — 前端集成：登录 / 余额 / 路由守卫

### 3.1 认证状态管理
- [ ] 新建 `src/common/stores/auth/useAuthStore.ts`（Zustand）：
  - 状态：`user`, `accessToken`, `isLoggedIn`, `coinBalance`
  - 动作：`login()`, `logout()`, `refreshToken()`, `updateBalance()`
  - 持久化：accessToken 存内存，refreshToken 存 localStorage

### 3.2 登录/注册页面
- [ ] 新建 `src/apps/auth/AuthPage.tsx`：
  - 登录 Tab + 注册 Tab（切换式）
  - 登录表单：邮箱 + 密码 + 记住我
  - 注册表单：邮箱 + 昵称 + 密码 + 确认密码
  - 炫酷数学风格背景（复用 MathWelcome 的审美）
- [ ] 新建 `pages/auth.tsx` 路由入口
- [ ] 实现表单校验与错误提示

### 3.3 全局路由守卫
- [ ] 修改 `pages/_app.tsx` 或全局布局组件：
  - 检查 `useAuthStore.isLoggedIn`
  - 未登录 + 非 `/auth` 页 → 重定向到 `/auth`
  - `/auth` 页 + 已登录 → 重定向到 `/`

### 3.4 侧边栏用户信息
- [ ] 修改导航组件（`DesktopNav` / `MobileNavItems`）：
  - 底部显示：用户头像 + 昵称
  - 金币余额徽章（实时从 store 读取）
  - 点击头像 → 展开菜单：个人中心、充值、退出登录
- [ ] 新建 `src/common/components/UserCoinBadge.tsx` 余额徽章组件

### 3.5 余额不足拦截
- [ ] 修改聊天发送逻辑（`Composer.tsx` 或消息发送处）：
  - 发送前调用 `coin.checkBalance`
  - 不足时弹出 `InsufficientBalanceDialog` 组件
- [ ] 修改生图发送逻辑（`BananaApp.tsx`）：
  - 同上预检查
- [ ] 新建 `src/common/components/InsufficientBalanceDialog.tsx`：
  - 显示"余额不足"提示 + 当前余额 + 所需金币
  - "去充值"按钮

### 3.6 移除前端 API Key 面板
- [ ] 移除或隐藏模型设置中的"API Key"输入框
- [ ] 将 API Key 改为后台环境变量统一管理（`env.server.ts` 中已有）

---

## Phase 4 — 管理后台：用户/充值/定价

### 4.1 管理后台路由与布局
- [ ] 新建 `src/apps/admin/AdminPage.tsx` 主布局：
  - 左侧导航：用户管理、模型定价、充值记录、统计概览
  - 仅 Admin 角色可访问
- [ ] 新建 `pages/admin.tsx` 路由入口

### 4.2 管理 tRPC Router
- [ ] 新建 `src/server/trpc/routers/admin.router.ts`：
  - [ ] `listUsers`：分页查用户列表（含搜索）
  - [ ] `rechargeUser`：给指定用户充值指定金币
  - [ ] `toggleUser`：启用/禁用用户
  - [ ] `listPricing`：查模型定价列表
  - [ ] `updatePricing`：修改模型单价
  - [ ] `createPricing`：新增模型定价
  - [ ] `getStats`：统计（总用户、活跃用户、总消费金币、总充值金额）
- [ ] 在 `trpc.router-edge.ts` 中注册 `admin` 路由（使用 `adminProcedure`）

### 4.3 用户管理页
- [ ] 新建 `src/apps/admin/UserManagement.tsx`：
  - 用户表格：邮箱、昵称、余额、角色、状态、注册时间
  - 操作：充值按钮（弹窗输入金币数）、启用/禁用
  - 搜索与分页

### 4.4 模型定价管理页
- [ ] 新建 `src/apps/admin/PricingManagement.tsx`：
  - 定价表格：模型ID、名称、类别、金币单价、状态
  - 操作：编辑价格、启用/禁用
  - 新增模型定价按钮

### 4.5 统计概览页
- [ ] 新建 `src/apps/admin/Dashboard.tsx`：
  - 卡片指标：总用户数、今日活跃、总消费金币、总充值金额
  - 最近 7 天折线图（后续迭代）

---

## Phase 5 — 支付与增值

### 5.1 支付集成准备
- [ ] 注册微信支付商户号（需要营业执照）
- [ ] 注册支付宝开发者账号 + 应用
- [ ] 获取支付密钥、证书等配置
- [ ] 在 `.env` 中配置支付相关环境变量

### 5.2 支付后端
- [ ] 安装支付 SDK：`wechatpay-node-v3` + `alipay-sdk`
- [ ] 新建 `src/server/services/payment.service.ts`：
  - [ ] `createWechatOrder(userId, amountYuan)` — 创建微信支付订单
  - [ ] `createAlipayOrder(userId, amountYuan)` — 创建支付宝订单
  - [ ] `handleWechatNotify(body)` — 处理微信回调
  - [ ] `handleAlipayNotify(body)` — 处理支付宝回调
  - 回调中：验签 → 更新订单状态 → 加金币 → 记流水
- [ ] 新建 `pages/api/pay/create.ts` — 创建订单 API
- [ ] 新建 `pages/api/pay/notify/wechat.ts` — 微信回调
- [ ] 新建 `pages/api/pay/notify/alipay.ts` — 支付宝回调
- [ ] 新建 `pages/api/pay/status/[orderNo].ts` — 查询订单状态

### 5.3 充值前端
- [ ] 新建 `src/common/components/RechargeDialog.tsx`：
  - 预设充值档位：1元(30币)、5元(150币)、10元(300币)、50元(1500币)
  - 支持自定义金额
  - 选择支付方式：微信/支付宝
  - 展示付款二维码（微信 Native）或跳转（支付宝 H5）
  - 轮询订单状态 → 充值成功提示

### 5.4 后续增值功能（备选）
- [ ] 邮箱验证注册
- [ ] 手机号验证码登录
- [ ] 新用户邀请奖励机制
- [ ] 按 Token 计费而非按次计费
- [ ] 用户 VIP 等级（折扣体系）
- [ ] 使用额度限制（防滥用）

---

## 执行优先级

```
Phase 1 (地基) ─→ Phase 2 (计费) ─→ Phase 3 (前端) ─→ Phase 4 (后台) ─→ Phase 5 (支付)
  ████████████       ██████████        ██████████        ████████           ████████
  约 3-4 天           约 2-3 天          约 3-4 天          约 2-3 天          约 3-4 天
```

> [!IMPORTANT]
> Phase 1-4 完成后系统即可上线使用（管理员手动充值）。Phase 5 的微信/支付宝接入需要商户资质审批，通常需要 3-7 个工作日，可与 Phase 3-4 的开发并行申请。

# 数据库巡检清单

这份清单用于生产环境日常巡检，适用于当前项目的 Docker + PostgreSQL + 阿里云 OSS 备份方案。

## 每天巡检

### 1. 检查容器状态

```bash
cd /www/wwwroot/aittco
docker compose ps
```

重点确认：

- `aittco-db` 为 `healthy`
- `aittco-backend` 为 `healthy`
- `aittco-frontend` 为 `Up`

### 2. 检查本地数据库备份日志

```bash
tail -n 50 /www/backup/aittco/postgres/backup.log
```

重点确认：

- 没有 `pg_dump` 报错
- 没有权限错误
- 没有磁盘写入失败

### 3. 检查 OSS 同步日志

```bash
tail -n 50 /www/backup/aittco/postgres/rclone.log
```

重点确认：

- 没有 `403`
- 没有认证失败
- 没有网络超时

### 4. 检查最新备份文件是否更新

```bash
ls -lh /www/backup/aittco/postgres | tail
```

重点确认：

- 当天是否生成新的 `aittcodb_时间.sql`
- `latest.sql` 修改时间是否更新

## 每周巡检

### 1. 检查 OSS 巡检日志

```bash
tail -n 50 /www/backup/aittco/postgres/oss-check.log
```

重点确认：

- 出现 `[oss-check] OK`
- 没有提示 OSS 缺少最新备份
- 没有提示本地备份过旧

### 2. 检查定时任务是否仍然存在

```bash
crontab -l
```

重点确认以下 3 条任务仍然存在：

- `03:30` 本地数据库备份
- `03:50` 同步到阿里云 OSS
- 每周一 `04:10` 检查 OSS 备份完整性

### 3. 检查 OSS 远端文件

```bash
rclone ls aliyunoss:aittco-db-backup/postgres
```

重点确认：

- `latest.sql` 存在
- 最近几天的 `aittcodb_*.sql` 存在

### 4. 检查服务器磁盘空间

```bash
df -h
```

重点确认：

- 系统盘没有接近满载
- `/www` 所在磁盘空间充足

## 发版后冻结结算验收（必跑）

用于验证“冻结余额 -> 成功结算 -> 失败解冻”是否正常，建议每次发版后执行一次。

### 1. 查看最近冻结记录

```bash
docker exec -i aittco-db psql -U aittcouser -d aittcodb -c \
"SELECT \"taskKey\",status,amount,\"createdAt\",\"settledAt\",\"releasedAt\" FROM \"CoinReservation\" ORDER BY \"createdAt\" DESC LIMIT 10;"
```

通过标准：

- 成功任务应为 `SETTLED`
- 失败任务应为 `RELEASED`
- 不应出现长时间 `PENDING`（超过 60 分钟）

### 2. 查看最近扣费流水

```bash
docker exec -i aittco-db psql -U aittcouser -d aittcodb -c \
"SELECT type,amount,balance,description,\"createdAt\" FROM \"CoinTransaction\" ORDER BY \"createdAt\" DESC LIMIT 20;"
```

通过标准：

- 成功生图后有 `type=CONSUME` 且 `amount` 为负数
- 失败任务不应新增对应扣费流水

### 3. 检查是否有超时未处理冻结单

```bash
docker exec -i aittco-db psql -U aittcouser -d aittcodb -c \
"SELECT COUNT(*) AS stale_pending FROM \"CoinReservation\" WHERE status='PENDING' AND \"createdAt\" < NOW() - INTERVAL '60 minutes';"
```

通过标准：

- `stale_pending = 0`

### 4. 成功场景手工验收（建议）

执行步骤：

1. 记录余额 `B0`
2. 发起一笔异步生图后立即刷新或关闭页面
3. 等待上游任务完成后，重新登录查看余额与流水

通过标准：

- 余额为 `B0 - 本次模型价格`
- `CoinReservation` 为 `SETTLED`

### 5. 失败场景手工验收（建议）

执行步骤：

1. 临时将测试模型密钥改错
2. 发起一笔异步生图后立即刷新或关闭页面
3. 查询 `CoinReservation` 与 `CoinTransaction`
4. 立刻改回正确密钥

通过标准：

- `CoinReservation` 为 `RELEASED`
- 没有新增该笔任务 of `CONSUME` 扣费

## 最核心的 5 条巡检命令

如果只做最小巡检，至少执行下面 5 条：

```bash
cd /www/wwwroot/aittco && docker compose ps
tail -n 50 /www/backup/aittco/postgres/backup.log
tail -n 50 /www/backup/aittco/postgres/rclone.log
tail -n 50 /www/backup/aittco/postgres/oss-check.log
df -h
```

## 出现这些情况要立刻处理

- `aittco-db` 不是 `healthy`
- `backup.log` 出现 `pg_dump` error
- `rclone.log` 出现 `403`、`Forbidden`、认证失败
- `oss-check.log` 没有出现 `OK`
- 磁盘空间低于 20%

## 相关文件

- [数据库安全与备份说明.md](/D:/aittco/数据库安全与备份说明.md)
- [数据库恢复应急流程.md](/D:/aittco/数据库恢复应急流程.md)
- [db-backup.sh](/D:/aittco/scripts/db-backup.sh)
- [check-oss-backup.sh](/D:/aittco/scripts/check-oss-backup.sh)
- [notify-webhook.sh](/D:/aittco/scripts/notify-webhook.sh)

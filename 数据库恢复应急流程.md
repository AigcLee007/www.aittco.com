# 数据库恢复应急流程

适用项目：`www.aittco.com`

目标：
- 在数据库异常、容器误删、数据损坏、服务器迁移时，快速恢复业务
- 优先恢复用户、充值、金币流水、模型定价、系统配置

## 一、先判断是哪种故障

### 1. 容器挂了，但数据库卷还在
现象：
- `aittco-db` 没启动
- `docker volume ls` 还能看到 `aittco_postgres_data`

处理思路：
- 先重启容器，不要急着恢复备份

命令：

```bash
cd /www/wwwroot/aittco
docker compose up -d aittco-db aittco-backend aittco-frontend
docker compose ps
```

### 2. 项目能启动，但数据明显不对
现象：
- 用户数异常
- 交易流水为空
- 模型定价丢失
- 系统配置异常

处理思路：
- 先核对当前连接的是不是正式卷
- 再决定是否恢复备份

命令：

```bash
cd /www/wwwroot/aittco
grep DB_VOLUME_NAME .env
docker volume ls
docker compose ps
```

应确认：
- 正式卷名是 `aittco_postgres_data`

### 3. 数据真的丢了，或者需要回滚
现象：
- 正式卷损坏
- 误清库
- 需要恢复到昨天或某个时间点

处理思路：
- 用本地 SQL 备份或 OSS 备份恢复

## 二、恢复前必须先做的事

### 1. 停掉应用写入

```bash
cd /www/wwwroot/aittco
docker compose stop aittco-frontend aittco-backend
```

### 2. 备份当前故障现场
即使当前库有问题，也先留一份现场，避免误覆盖。

```bash
mkdir -p /www/backup/aittco/emergency
docker exec aittco-db pg_dump -U aittcouser -d aittcodb --clean --if-exists --no-owner --no-privileges > /www/backup/aittco/emergency/before_restore_$(date +%F_%H%M%S).sql
```

## 三、从本地备份恢复

本地备份目录：

```text
/www/backup/aittco/postgres
```

常用文件：
- `latest.sql`
- `aittcodb_YYYY-MM-DD_HHMMSS.sql`

### 1. 确认备份文件存在

```bash
ls -lh /www/backup/aittco/postgres
```

### 2. 恢复数据库

使用最近一次备份：

```bash
cat /www/backup/aittco/postgres/latest.sql | docker exec -i aittco-db psql -U aittcouser -d aittcodb
```

如果要恢复指定时间点：

```bash
cat /www/backup/aittco/postgres/aittcodb_2026-03-26_180346.sql | docker exec -i aittco-db psql -U aittcouser -d aittcodb
```

### 3. 恢复后重启应用

```bash
cd /www/wwwroot/aittco
docker compose up -d aittco-backend aittco-frontend
docker compose ps
```

## 四、从 OSS 恢复

OSS 路径：

```text
aliyunoss:aittco-db-backup/postgres
```

### 1. 先从 OSS 拉回服务器

恢复最新备份：

```bash
mkdir -p /www/backup/aittco/restore
rclone copy aliyunoss:aittco-db-backup/postgres/latest.sql /www/backup/aittco/restore
```

如果要先看远端有哪些备份：

```bash
rclone ls aliyunoss:aittco-db-backup/postgres
```

### 2. 执行恢复

```bash
cat /www/backup/aittco/restore/latest.sql | docker exec -i aittco-db psql -U aittcouser -d aittcodb
```

### 3. 重启应用

```bash
cd /www/wwwroot/aittco
docker compose up -d aittco-backend aittco-frontend
docker compose ps
```

## 五、恢复后检查清单

恢复完成后，至少执行下面这些检查。

### 1. 容器状态

```bash
cd /www/wwwroot/aittco
docker compose ps
```

### 2. 核心业务表计数

```bash
docker exec -i aittco-db psql -U aittcouser -d aittcodb <<'SQL'
select count(*) as users from "User";
select count(*) as coin_transactions from "CoinTransaction";
select count(*) as payment_orders from "PaymentOrder";
select count(*) as model_pricing from "ModelPricing";
select count(*) as system_config from "SystemConfig";
SQL
```

### 3. 前后端健康检查

```bash
curl -I http://127.0.0.1:3333
curl http://127.0.0.1:3336/healthz
```

### 4. 手工检查页面
- 用户能否登录
- 金币中心是否正常
- 管理后台用户管理是否正常
- 交易流水是否存在
- 模型定价是否正常

## 六、常见故障与处理

### 1. `P1000 Authentication failed`
原因：
- `.env` 里的数据库密码与实际数据库密码不一致

优先处理：
- 核对 `.env` 中：
  - `POSTGRES_PASSWORD`
  - `POSTGRES_PRISMA_URL`
  - `POSTGRES_URL_NON_POOLING`

### 2. `P1001 Can't reach database`
原因：
- `aittco-db` 没启动
- 连接串写错
- Docker 网络异常

优先处理：

```bash
docker compose ps
docker logs aittco-db --tail 100
```

### 3. 前端 502
原因：
- `aittco-frontend` 重启失败
- Nginx 反代到 3333 时上游不可用

优先处理：

```bash
docker logs aittco-frontend --tail 120
docker compose ps
```

## 七、绝对不要直接执行的命令

生产环境不要直接执行：

```bash
docker compose down -v
docker system prune --volumes -f
prisma db push --force-reset
```

这些命令可能直接删库或删卷。

## 八、建议的恢复顺序

1. 先停前后端，防止继续写入
2. 备份当前故障现场
3. 优先尝试本地备份恢复
4. 本地备份不可用时，再从 OSS 拉回恢复
5. 恢复后检查核心表和页面
6. 确认正常后，再恢复业务访问

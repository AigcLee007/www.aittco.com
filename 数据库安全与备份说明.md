# 数据库安全与备份说明

## 当前保护措施

1. Docker 数据卷已固定名称
   - `DB_VOLUME_NAME=math_postgres_data`
   - 避免因为 Compose 项目名变化而误创建新的空卷

2. 前端容器启动时会自动执行
   - `prisma db push`
   - `seed.ts`
   - 数据库安全自检

3. 安全自检会检查以下关键数据
   - 用户数
   - 模型定价数
   - 系统配置数
   - 充值套餐数

4. 如果核心配置为空
   - 默认会阻止前端继续启动
   - 防止系统在“疑似空库”状态下静默上线

## 常用命令

### 1. 手动备份数据库

```powershell
npm run db:backup
```

备份目录：

```text
D:\math\backups\postgres
```

### 2. 恢复最近一次备份

```powershell
npm run db:restore:latest
```

### 3. 注册每天自动备份任务

```powershell
npm run db:backup:task
```

默认每天 `03:30` 执行一次。

### 4. 每周校验 OSS 备份是否存在

Linux 服务器可直接执行：

```bash
chmod +x /www/wwwroot/math/scripts/check-oss-backup.sh
/www/wwwroot/math/scripts/check-oss-backup.sh
```

### 5. 备份失败自动报警

建议配置一个通用 Webhook 地址，例如：
- 企业微信机器人
- 钉钉机器人
- 飞书机器人
- Server酱

在服务器环境里先设置：

```bash
export BACKUP_ALERT_WEBHOOK_URL="你的告警 webhook 地址"
```

或写入定时任务前缀：

```bash
BACKUP_ALERT_WEBHOOK_URL="你的告警 webhook 地址" /www/wwwroot/math/scripts/db-backup.sh
```

已经支持自动报警的脚本：
- `/www/wwwroot/math/scripts/db-backup.sh`
- `/www/wwwroot/math/scripts/check-oss-backup.sh`

建议的定时任务示例：

```bash
30 3 * * * BACKUP_ALERT_WEBHOOK_URL="你的告警 webhook 地址" /www/wwwroot/math/scripts/db-backup.sh >> /www/backup/math/postgres/backup.log 2>&1
50 3 * * * rclone sync /www/backup/math/postgres aliyunoss:math-db-backup/postgres >> /www/backup/math/postgres/rclone.log 2>&1
10 4 * * 1 BACKUP_ALERT_WEBHOOK_URL="你的告警 webhook 地址" /www/wwwroot/math/scripts/check-oss-backup.sh >> /www/backup/math/postgres/oss-check.log 2>&1
```

建议加入每周巡检任务，例如每周一早上 `04:10`：

```bash
(crontab -l 2>/dev/null; echo "10 4 * * 1 /www/wwwroot/math/scripts/check-oss-backup.sh >> /www/backup/math/postgres/oss-check.log 2>&1") | crontab -
```

查看巡检日志：

```bash
tail -n 50 /www/backup/math/postgres/oss-check.log
```

## 备份文件说明

- 每次备份会生成一个时间戳文件
- 同时覆盖一份 `latest.sql`
- 默认保留最近 `14` 天

## 风险操作提醒

不要执行以下命令：

```powershell
docker compose down -v
```

这会删除数据库卷。

## 建议操作顺序

1. 大改模型、支付、用户体系前先执行一次 `npm run db:backup`
2. 确认备份文件生成成功后再改
3. 发布后检查后台：
   - 用户管理
   - 交易流水
   - 模型定价
   - 系统设置

# 数据库安全与备份说明

## 当前保护措施

1. Docker 数据卷已固定名称
   - `DB_VOLUME_NAME=math_postgres_data`
   - 避免因为 Compose 项目名变化而误创建新的空卷

2. 前端容器启动时会自动执行
   - `prisma db push`
   - `seed.ts`
   - 数据库安全自检

3. 安全自检会检查以下关键数据
   - 用户数
   - 模型定价数
   - 系统配置数
   - 充值套餐数

4. 如果核心配置为空
   - 默认会阻止前端继续启动
   - 防止系统在“疑似空库”状态下静默上线

## 常用命令

### 1. 手动备份数据库

```powershell
npm run db:backup
```

备份目录：

```text
D:\math\backups\postgres
```

### 2. 恢复最近一次备份

```powershell
npm run db:restore:latest
```

### 3. 注册每天自动备份任务

```powershell
npm run db:backup:task
```

默认每天 `03:30` 执行一次。

### 4. 每周校验 OSS 备份是否存在

Linux 服务器可直接执行：

```bash
chmod +x /www/wwwroot/math/scripts/check-oss-backup.sh
/www/wwwroot/math/scripts/check-oss-backup.sh
```

### 5. 备份失败自动报警

建议配置一个通用 Webhook 地址，例如：
- 企业微信机器人
- 钉钉机器人
本项目采用多级备份方案，确保数据的安全性和极端情况下的可恢复性。数据备份的重点是：**用户余额、充值记录、金币流水、模型定价、系统配置**。

## 一、备份级别

1. **L1 本地实时备份**：Docker 卷持久化存储，宿主机对应目录：`/var/lib/docker/volumes/aittco_postgres_data/_data`。
2. **L2 本地定时快照**：每 24 小时执行一次 `pg_dump`，保留最近 7 天的 SQL 文件。
   - 存储路径：`/www/backup/aittco/postgres/`
3. **L3 云端异地容灾**：每天凌晨自动将本地 SQL 备份同步至阿里云 OSS，保留最近 30 天。
   - OSS 路径：`aliyunoss:aittco-db-backup/postgres/`

## 二、自动备份机制

### 1. 备份脚本
脚本路径：`scripts/db-backup.sh`

该脚本会自动检测 `aittco-db` 容器，执行全量备份，并清理 7 天前的旧备份。

### 2. 定时任务 (Cron)
建议在宿主机设置以下定时任务：

```bash
# 每天凌晨 03:30 执行本地数据库备份
30 3 * * * /bin/bash /www/wwwroot/aittco/scripts/db-backup.sh >> /www/backup/aittco/postgres/backup.log 2>&1

# 每天凌晨 03:50 将备份同步至 OSS
50 3 * * * rclone sync /www/backup/aittco/postgres aliyunoss:aittco-db-backup/postgres >> /www/backup/aittco/postgres/rclone.log 2>&1

# 每周一凌晨 04:10 检查 OSS 备份完整性并发送通知
10 4 * * 1 /bin/bash /www/wwwroot/aittco/scripts/check-oss-backup.sh >> /www/backup/aittco/postgres/oss-check.log 2>&1
```

## 三、安全加固建议

### 1. 数据库访问控制
- **防火墙**：不要暴露 3339 端口到公网。如果必须远程连接，请使用 SSH 隧道或配置特定的 IP 白名单。
- **密码强度**：`.env` 中的 `POSTGRES_PASSWORD` 必须使用 16 位以上强随机字符串。

### 2. 灾难恢复演练
建议每季度按照 [数据库恢复应急流程.md](数据库恢复应急流程.md) 执行一次恢复演练，验证备份文件的有效性。

## 四、备份完整性检查 (OSS)
我们使用 `check-oss-backup.sh` 配合 `rclone` 自动检查远端是否存在最新的备份文件。如果检查失败，会通过 `notify-webhook.sh` 向您的机器人发送警报。

## 五、关键配置核对表
| 项 | 预期值 |
| :--- | :--- |
| Docker 项目名 | `Aittco` |
| DB服务名 | `aittco-db` |
| DB用户名 | `aittcouser` |
| DB库名 | `aittcodb` |
| 备份目录 | `/www/backup/aittco/postgres` |
| OSS 命名空间 | `aittco-db-backup` |

## 备份文件说明

- 每次备份会生成一个时间戳文件
- 同时覆盖一份 `latest.sql`
- 默认保留最近 `14` 天

## 风险操作提醒

不要执行以下命令：

```powershell
docker compose down -v
```

这会删除数据库卷。

## 建议操作顺序

1. 大改模型、支付、用户体系前先执行一次 `npm run db:backup`
2. 确认备份文件生成成功后再改
3. 发布后检查后台：
   - 用户管理
   - 交易流水
   - 模型定价
   - 系统设置

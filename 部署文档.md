# 宝塔面板部署与更新指南（最简版，非 Docker，无数据库）

本文档是本项目在宝塔面板上从零部署到后续更新维护的全流程操作指南。本项目包含 **Next.js 15 主服务** 与 **Python FastAPI 生图后端**，无需数据库即可运行（采用浏览器本地存储）。

---

## 🟢 第一部分：首次服务器部署

### 1. 服务器环境准备（安装依赖软件）

登录宝塔面板，在【软件商店】搜索并安装：

1. **Nginx**（建议版本 1.20 或以上）：用于绑定域名和反向代理。
2. **Node.js 版本管理器**：安装 **Node 20.x 及以上** 版本并设为全局，将镜像源设为“淘宝 NPM 镜像”。
3. **PM2 管理器**：Node.js 项目最强大的后台进程管理工具。

### 2. 本地项目打包上传

1. 在本地电脑的项目根目录下，全选所有内容并**压缩为 zip 文件**（如 `aittco.zip`）。
   > ❌ **绝对不要打包** 巨大的 `.next`、`node_modules` 和 `backend/__pycache__` 文件夹。
2. 在宝塔中建立节点：点击【网站】 -> 【Node 项目】 -> 【添加 Node 项目】。
   - **项目目录**：`/www/wwwroot/www.aittco.com`
   - **项目名称**：`aittco_frontend`
   - **启动选项**：`start:next start`
   - **Node版本**：`v20.x 及以上`
   - **运行端口**：`3333`
   - **实例数 (Instances)**：强烈建议填 `1` (或使用 Fork 模式)。
     > [!IMPORTANT]
     > 如果实例数大于 1 (集群模式)，会导致生图任务在不同进程间丢失，从而报错“格式不正确”。
3. 提交后，进入该 `/www/wwwroot/www.aittco.com` 目录，将刚才的 `aittco.zip` 上传并解压覆盖。

### 3. 配置环境变量

在目录 `/www/wwwroot/www.aittco.com` 下新建或者编辑 `.env` 文件：

```env
# 取消数据库配置，留空即可
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=

# 主要填写你需要用到的各种大模型 API KEY
OPENAI_API_KEY="sk-你的APIKEY"
# ...
```

### 4. 终端部署 Python 生图后端 (端口 3336)

1. 在宝塔【文件】界面进入 `/www/wwwroot/www.aittco.com/backend` 目录，点击右上角**【终端】**。
2. 依次执行执行以下命令（为保证稳定，我们不使用宝塔 Python 管理器，全程手动建环境并用 PM2 托管）：

```bash
# 更新并安装 Python3 虚拟环境组件（如果是 Ubuntu/Debian）
apt update && apt install -y python3.10-venv python3-pip

# 创建并进入独立虚拟环境
python3 -m venv venv
source venv/bin/activate

# 使用国内源闪速安装依赖
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 使用 PM2 接管后端服务并挂载常驻
npx pm2 start main.py --name "aittco_backend" --interpreter ./venv/bin/python
```

完成后即可关掉终端，后端功能已在内网稳健运行。

### 5. 终端构建前端 Main 项目

1. 回到上一层前端目录 `/www/wwwroot/www.aittco.com`，打开**【终端】**。
2. 执行以下命令：

```bash
# 1. 安装模块
npm install --registry=https://registry.npmmirror.com

# 2. 生成本地 Prisma Client 结构映射（必须运行）
npx prisma generate

# 3. 扩大节点内存限制，防止编译大项目爆内存
export NODE_OPTIONS="--max-old-space-size=4096"

# 4. 执行正式打包构建（需要几分钟耐心等待，不要关闭）
npm run build
```

### 6. PM2 启动主项目与绑定域名

1. 回到宝塔的 **【网站】** -> **【Node 项目】**。![alt text](image.png)
2. 找到你的 `aittco_frontend`，点一下**【重启】**（或启动），确保它处于绿色的“运行中”状态。
3. 给这套服务套上安全的 HTTPS 域名：
   - 到【网站】 -> 【PHP 网站】 -> 【添加站点】，填入你的域名 `www.aittco.com`。
   - 打开新建网站的【设置】 -> 【SSL】 -> 【Let's Encrypt】，申请证书并开启并强制 HTTPS。
   - 切到【反向代理】 -> 【添加反向代理】 -> 目标 URL 填 `http://127.0.0.1:3333` -> 发送域名填 `$host` -> 保存提交。
     _(可选：可以点击反代配置文件，添加 `proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade";` 支持 websocket）_

至此，初次部署大功告成，可直接访问域名。

---

## 🔵 第二部分：项目日常更新维护

当你在本地对代码（无论前端还是后端）进行了修改，随时可以按这个流程热更新服务器：

### 1. 更新文件

1. 像第一次一样，在本地打包最新的代码（忽略 `.next` 和 `node_modules`）。
2. 在宝塔进入项目目录 `/www/wwwroot/www.aittco.com`，上传 ZIP 压缩包并选择**解压覆盖所有**。

### 2. 执行编译与重启

打开终端，**只有**在对应的前/后端都执行了正确的更新命令并重启 PM2 后，新代码才会生效。

**👉 场景 A：如果你只修改了前端界面逻辑代码**

1. 在项目根目录终端执行：

   ```bash
   # 如果你新增了 npm 包，就先跑一下安装
   npm install --registry=https://registry.npmmirror.com

   # 如果你修改了数据库 Schema 文件，必须跑
   npx prisma generate

   # 重新打包构建
   export NODE_OPTIONS="--max-old-space-size=4096"
   npm run build
   ```

2. 构建成功后，去 **【软件商店】->【PM2 管理器】**，找到名叫 `math_frontend` (或 `math`) 的进程，点击右侧的**重启**按钮。

**👉 场景 B：如果你修改了 backend 里的 Python 接口**

1. 在项目终端中主动进入 `backend` 目录，更新环境：
   ```bash
   cd backend
   source venv/bin/activate
   # （如有依赖更新）
   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
   ```
2. 去 **【软件商店】->【PM2 管理器】**，找到名叫 `math_backend` 的进程，点击右侧的**重启**按钮。

更新完毕！刷新浏览器检查效果即可。
